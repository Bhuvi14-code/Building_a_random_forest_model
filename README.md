# Building_a_random_forest_model
This notebook builds a Random Forest model to predict airline customer satisfaction. It cleans and encodes survey data, tunes model parameters with GridSearchCV, makes test predictions, and evaluates performance using precision, recall, accuracy, and F1-score.
